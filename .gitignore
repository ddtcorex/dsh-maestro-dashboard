node_modules
.DS_Store
*.log

# tsc client emit dir (esbuild reads src directly)
.client-build/

# Build output — regenerated by pnpm build, never commit
lib/

# Re-include coordination files ignored by the global core.excludesFile
!AGENTS.md
!CLAUDE.md
